import AbstractDeleteUserCancelAction from "../../../gen/admin/actions/AbstractDeleteUserCancelAction";

export default class DeleteUserCancelAction extends AbstractDeleteUserCancelAction {

    initActionData() {
    	//add not replayable data to action data in order to freeze for replay (e.g. time or date)
    }

}

/*       S.D.G.       */
