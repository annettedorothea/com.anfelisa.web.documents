import AbstractRemoveEditedCardImageAction from "../../../gen/card/actions/AbstractRemoveEditedCardImageAction";

export default class RemoveEditedCardImageAction extends AbstractRemoveEditedCardImageAction {

    initActionData() {
    	//add not replayable data to action data in order to freeze for replay (e.g. time or date)
    }

}

/*       S.D.G.       */
