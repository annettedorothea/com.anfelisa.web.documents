import AbstractToggleInputOrderAction from "../../../gen/card/actions/AbstractToggleInputOrderAction";

export default class ToggleInputOrderAction extends AbstractToggleInputOrderAction {

    initActionData() {
    	//add not replayable data to action data in order to freeze for replay (e.g. time or date)
    }

}

/*       S.D.G.       */
