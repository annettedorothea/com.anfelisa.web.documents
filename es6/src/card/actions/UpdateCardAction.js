import AbstractUpdateCardAction from "../../../gen/card/actions/AbstractUpdateCardAction";

export default class UpdateCardAction extends AbstractUpdateCardAction {

    initActionData() {
    	//add not replayable data to action data in order to freeze for replay (e.g. time or date)
    }

}

/*       S.D.G.       */
