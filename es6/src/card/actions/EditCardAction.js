import AbstractEditCardAction from "../../../gen/card/actions/AbstractEditCardAction";

export default class EditCardAction extends AbstractEditCardAction {

    initActionData() {
    	//add not replayable data to action data in order to freeze for replay (e.g. time or date)
    }

}

/*       S.D.G.       */
