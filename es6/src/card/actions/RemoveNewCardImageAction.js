import AbstractRemoveNewCardImageAction from "../../../gen/card/actions/AbstractRemoveNewCardImageAction";

export default class RemoveNewCardImageAction extends AbstractRemoveNewCardImageAction {

    initActionData() {
    	//add not replayable data to action data in order to freeze for replay (e.g. time or date)
    }

}

/*       S.D.G.       */
