import AbstractLoadCardsAction from "../../../gen/card/actions/AbstractLoadCardsAction";

export default class LoadCardsAction extends AbstractLoadCardsAction {

    initActionData() {
    	//add not replayable data to action data in order to freeze for replay (e.g. time or date)
    }

}

/*       S.D.G.       */
