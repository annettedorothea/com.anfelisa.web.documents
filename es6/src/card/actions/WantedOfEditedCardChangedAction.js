import AbstractWantedOfEditedCardChangedAction from "../../../gen/card/actions/AbstractWantedOfEditedCardChangedAction";

export default class WantedOfEditedCardChangedAction extends AbstractWantedOfEditedCardChangedAction {

    initActionData() {
    	//add not replayable data to action data in order to freeze for replay (e.g. time or date)
    }

}

/*       S.D.G.       */
