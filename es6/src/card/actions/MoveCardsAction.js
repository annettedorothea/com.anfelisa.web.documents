import AbstractMoveCardsAction from "../../../gen/card/actions/AbstractMoveCardsAction";

export default class MoveCardsAction extends AbstractMoveCardsAction {

    initActionData() {
    	//add not replayable data to action data in order to freeze for replay (e.g. time or date)
    }

}

/*       S.D.G.       */
