import AbstractUpdateHashEvent from "../../../gen/common/events/AbstractUpdateHashEvent";

export default class UpdateHashEvent extends AbstractUpdateHashEvent {
    prepareDataForView() {
        this.eventData = JSON.parse(JSON.stringify(this.eventParam));
        if (this.eventData.data === undefined) {
        	this.eventData.data = {};
        }
    }
}

/*       S.D.G.       */
