import AbstractDeleteCategoryAction from "../../../gen/category/actions/AbstractDeleteCategoryAction";

export default class DeleteCategoryAction extends AbstractDeleteCategoryAction {

    initActionData() {
    	//add not replayable data to action data in order to freeze for replay (e.g. time or date)
    }

}

/*       S.D.G.       */
