import AbstractCloseInviteUserAction from "../../../gen/category/actions/AbstractCloseInviteUserAction";

export default class CloseInviteUserAction extends AbstractCloseInviteUserAction {

    initActionData() {
    	//add not replayable data to action data in order to freeze for replay (e.g. time or date)
    }

}

/*       S.D.G.       */
