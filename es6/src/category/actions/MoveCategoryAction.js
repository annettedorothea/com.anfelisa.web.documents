import AbstractMoveCategoryAction from "../../../gen/category/actions/AbstractMoveCategoryAction";

export default class MoveCategoryAction extends AbstractMoveCategoryAction {

    initActionData() {
    	//add not replayable data to action data in order to freeze for replay (e.g. time or date)
    }

}

/*       S.D.G.       */
