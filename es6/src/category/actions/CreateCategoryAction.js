import AbstractCreateCategoryAction from "../../../gen/category/actions/AbstractCreateCategoryAction";

export default class CreateCategoryAction extends AbstractCreateCategoryAction {

    initActionData() {
    	//add not replayable data to action data in order to freeze for replay (e.g. time or date)
    }

}

/*       S.D.G.       */
