import AbstractEditMaxCardsPerDayAction from "../../../gen/box/actions/AbstractEditMaxCardsPerDayAction";

export default class EditMaxCardsPerDayAction extends AbstractEditMaxCardsPerDayAction {

    initActionData() {
    	//add not replayable data to action data in order to freeze for replay (e.g. time or date)
    }

}

/*       S.D.G.       */
